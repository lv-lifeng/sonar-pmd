name: DependencyEye

on:
  pull_request:
    branches:
        - test_GHA


jobs:
  build-dep-eye:
    name: Build DependencyEye
    runs-on: ubuntu-latest
    steps:
      - name: ll
        run: ll
      - name: Dependency Eye
        uses: lv-lifeng/eye@v1.0.2
